// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDkmbk73C6V4Q9NHkPkXuTLQnZB1ca2e4Y',
    authDomain: 'monaco-cote-d-azur.firebaseapp.com',
    databaseURL: 'https://monaco-cote-d-azur.firebaseio.com',
    projectId: 'monaco-cote-d-azur',
    storageBucket: 'monaco-cote-d-azur.appspot.com',
    messagingSenderId: '476513075588'
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
