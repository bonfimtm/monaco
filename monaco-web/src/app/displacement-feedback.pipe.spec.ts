import { DisplacementFeedbackPipe } from './displacement-feedback.pipe';

describe('DisplacementFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplacementFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
